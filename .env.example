# Toggle full mock mode (all Axios requests routed to src/services/mockApi.ts)
VITE_USE_MOCK=true

# Mock scenario mode: success | empty | error
VITE_MOCK_SCENARIO=success

# When using real APIs
VITE_API_URL=http://localhost:8081/optics
